# user management

A minimal Node.js + TypeScript starter for the "user management" project.

## Setup

Install dependencies:

```bash
npm install
```

## Development

Run in dev mode (auto-reload):

```bash
npm run dev
```

Build and run:

```bash
npm run build
npm start
```

Server runs on port `3000` by default.
